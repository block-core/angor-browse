<Router AppAssembly="@typeof(App).Assembly" AdditionalAssemblies="new[]
{
    typeof(Angor.Browse.UniSatWallet.UniSatWallet).Assembly,
    typeof(Angor.Browse.BlockcoreWallet.BlockcoreWallet).Assembly,
    typeof(Angor.Browse.BlockcoreDns.BlockcoreDns).Assembly

}">
    <Found Context="routeData">
        <RouteView RouteData="@routeData" DefaultLayout="@typeof(MainLayout)" />
        <FocusOnNavigate RouteData="@routeData" Selector="h1" />
    </Found>
    <NotFound>
        <PageTitle>Not found</PageTitle>
        <LayoutView Layout="@typeof(MainLayout)">
            <p role="alert">Sorry, there's nothing at this address.</p>
        </LayoutView>
    </NotFound>
</Router>
